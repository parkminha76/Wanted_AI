# 인포가드 팀 규칙

2026 원티드 AI CHAMPIONSHIP · 4인 · 제출 9/20

규칙이 많으면 아무도 안 지킨다. 여기 있는 것은 **어기면 실제로 사고가 나는 것**만 남긴 목록이다.
바꾸고 싶으면 이 파일을 고쳐서 PR을 올리고, 나머지 세 명이 보면 된다.

---

## 0. 가장 중요한 세 가지

지키지 못할 규칙이 생기면 아래 세 개만 남기고 나머지는 버려도 된다.

1. **실제 개인정보는 저장소에 한 건도 넣지 않는다.** 개인정보 보호 서비스가 개인정보를 유출하면 그 순간 끝이다.
2. **`backend/shared/schema.py`의 기존 필드는 혼자 바꾸지 않는다.** 네 명이 이 파일 하나로 연결되어 있다.
3. **9/13에는 미완성이어도 배포한다.** 완성 후 배포가 아니라 배포 후 완성.

---

## 1. 협업 규칙

### 커밋 전에

- 산출물에 오타가 없는지 확인한다. 특히 **화면에 노출되는 문구** — 심사위원이 본다.
- 실제 개인정보·API 키가 섞이지 않았는지 `git diff`를 눈으로 훑는다.
- 남의 담당 파일을 고쳤다면 커밋 메시지에 이유를 쓴다.

### 브랜치와 push

- 각자 자기 이름 브랜치에서 작업한다. `main`에 직접 push하지 않는다.
- `main`에 합칠 때는 **PR을 올리고 팀 채팅에 한 줄 남긴다.** 리뷰 승인을 기다릴 필요는 없다(2주짜리다). 알리기만 하면 된다.
- 남의 브랜치를 force push 하지 않는다. 자기 브랜치도 남이 받아간 뒤에는 하지 않는다.
- 합치기 전에 `git pull --rebase`로 최신을 받아온다.

### 구조를 바꿀 때

- **폴더 구조를 바꾸면 반드시 팀에 공유한다.** 남의 import가 조용히 깨진다.
- 파일을 옮기거나 이름을 바꿀 때는 `git mv`를 쓴다. 지우고 새로 만들면 히스토리가 끊긴다.
- 폴더에는 주인이 있다. 주인이 아닌 사람은 그 폴더 안에서 구조를 바꾸지 않는다.

| 폴더 | 주인 |
|---|---|
| `backend/scanner/` | B (민하·윤정) |
| `ml/`, `sample_data/` | A (태민) |
| `backend/training/`, `prompts/` | C (은하) |
| `frontend/`, `infra/` | D (다같이 — 실제 커밋은 화면 담당) |
| `backend/shared/schema.py` | **공용. 4명 합의 후에만 기존 필드 변경** |
| `docs/` | 누구나. 결정을 남기는 자리 |

### 막혔을 때

- **두 시간 넘게 같은 자리에서 못 나가면 팀 채팅에 올린다.** 혼자 붙잡고 있는 게 팀에 가장 손해다.
- 매일 짧게 한 번 — 어제 한 것 / 오늘 할 것 / 막힌 것 세 줄.
- 대화로 정한 결정은 **문서에 남긴다.** 채팅에만 있으면 사흘 뒤에 아무도 기억하지 못한다.

---

## 2. 파일·코드 규칙

### 이름

- 변수·함수·클래스·파일명은 **영어만** 쓴다. 주석과 문서는 한국어로 써도 된다.
- 파이썬은 `snake_case`, 클래스는 `PascalCase`, 상수는 `UPPER_SNAKE`.
- 파일명에 **공백과 한글을 쓰지 않는다.** 배포 서버(리눅스)에서 인코딩이 깨진다. 문서 파일은 예외로 둔다.
- 임시 파일은 `test.py`, `temp.py` 같은 이름 대신 `scratch/` 폴더에 넣는다(git 제외).

### 인코딩 (팀 전원 Windows라 중요)

- 모든 소스 파일은 **UTF-8**로 저장한다.
- 저장소 루트에 `.gitattributes`를 두고 줄바꿈을 통일한다. 이게 없으면 아무것도 안 고쳤는데 파일 전체가 바뀐 것으로 보인다.

```
* text=auto eol=lf
*.png binary
*.pdf binary
*.xlsx binary
*.docx binary
```

### 의존성 — uv로 통일

패키지 관리는 **uv**를 쓴다. `pip install`을 직접 치지 않는다.

| 상황 | 명령 |
|---|---|
| 저장소를 처음 받았을 때 | `uv sync` |
| 남이 패키지를 추가한 뒤 | `uv sync` |
| 패키지를 새로 추가할 때 | `uv add pymupdf` |
| 개발용(테스트 등)만 추가 | `uv add --dev pytest` |
| 코드 실행 | `uv run python -m backend.main` |

- **`pyproject.toml`과 `uv.lock`을 둘 다 커밋한다.** `uv.lock`이 곧 "버전 고정"이다. 이 파일이 있어야 네 명이 완전히 같은 버전을 쓴다. `.gitignore`에 넣으면 안 된다.
- **`pyproject.toml`을 손으로 편집하지 않는다.** `uv add`를 쓰면 `pyproject.toml`과 `uv.lock`이 같이 갱신된다. 손으로 고치면 lock과 어긋난다.
- 패키지를 추가했으면 **그날 안에 두 파일을 push하고 팀에 알린다.** 받는 사람은 `uv sync` 한 번이면 끝난다.
- **Python은 3.12로 고정한다.** 저장소 루트의 `.python-version`에 `3.12`가 적혀 있고 uv가 알아서 그 버전을 받아온다. 각자 다른 버전을 쓰면 "제 컴퓨터에선 되는데요"가 시작된다.
- `.venv/`는 커밋하지 않는다. uv가 `uv sync`로 다시 만든다.

**`torch`는 CPU 전용 빌드로 받는다 (적용 완료).** `pyproject.toml`의 `[[tool.uv.index]]` /
`[tool.uv.sources]`가 그 설정이다. **이 블록을 지우지 말 것.**

기본 PyPI의 torch는 CUDA 런타임을 함께 끌고 온다 — 처음 lock에는 torch 본체 555MB에
`nvidia-*` 패키지 16개 약 4.7GB가 붙어 있었다. 배포 서버는 GPU를 쓰지 않는데(추론만 한다)
이 용량 때문에 무료 배포가 실패하고, "링크가 10/5까지 살아 있어야 한다"는 조건이 여기서 걸린다.

학습은 각자 로컬 GPU나 Colab에서 하고, 배포되는 것은 추론뿐이다.
로컬에서 GPU가 필요한 사람은 설정을 지우지 말고 `uv sync --no-sources`를 쓴다.

**배포 플랫폼이 `requirements.txt`를 요구하면** lock에서 뽑아 쓴다. 손으로 관리하지 않는다.

```bash
uv export --no-hashes --no-dev -o requirements.txt
```

### git에 넣지 않는 것

- **모델 가중치(`.pt`, `.bin`, `.safetensors`, `.onnx`)** — 저장소가 순식간에 수 GB가 된다. 공유는 HuggingFace Hub나 구글 드라이브로 하고, 코드에는 **경로가 아니라 설정값**으로 넣는다.
- **10MB 넘는 파일** — 넣기 전에 팀에 묻는다.
- **`.env`와 API 키** — `.env.example`에 키 이름만 적어 커밋한다. 값은 절대 안 된다.
- **업로드된 사용자 파일** — 애초에 저장하지 않는 것이 제품 원칙이지만, 개발 중 임시 폴더가 커밋되는 사고가 흔하다.
- **주피터 노트북의 출력** — 출력에 개인정보가 남는다. 커밋 전에 지운다(`Cell → All Output → Clear`).

`.gitignore`는 이미 저장소에 있고 꽤 촘촘하다. 새로 만들지 말고 **아래 세 줄만 추가**하면 된다.

```gitignore
# HuggingFace 모델 가중치 — *.pt/*.onnx만으로는 안 잡힌다
*.bin
*.safetensors

# 스캐너가 만드는 임시 산출물 (업로드 원본 · 마스킹 사본 · 배치 zip)
uploads/
outputs/
scratch/
```

⚠️ **함정 하나** — 지금 `.gitignore`에 `*.pdf`가 전역으로 걸려 있고 `!docs/**/*.pdf`만 예외다.
그런데 심사위원용 데모 샘플 4개 중 **계약서 PDF는 `sample_data/`에 들어간다.** 이대로면
그 파일이 조용히 커밋에서 빠지고, 발표 당일 "샘플 3개만 동작"하는 사고가 난다.
예외를 하나 더 넣어야 한다.

```gitignore
!sample_data/**/*.pdf
```

`*.csv`도 같은 구조인데 이쪽은 `!sample_data/**/*.csv` 예외가 이미 들어가 있다.

### 로그

- **로그에 원문을 남기지 않는다.** 탐지된 값은 `홍길동` 대신 `person(len=3)` 형태로 남긴다.
- 디버깅 중 `print(raw_text)`를 넣었으면 커밋 전에 지운다.

---

## 3. 데이터 규칙

### 실제 개인정보 금지

- 학습·테스트·데모 어디에도 실제 개인정보를 쓰지 않는다. **전부 Faker(ko_KR) 또는 직접 만든 가상 값**이다.
- 실제 문서(계약서, 이력서 등)를 참고할 때는 **구조만 보고 값은 전부 갈아 끼운다.**
- 커밋 전 확인용:

```bash
git diff --cached | grep -nE '01[016789]-?[0-9]{3,4}-?[0-9]{4}|[0-9]{6}-[1-4][0-9]{6}'
```

- 실수로 커밋했다면 **파일만 지우는 것으로는 부족하다.** 히스토리에 남는다. 즉시 팀에 알리고 함께 처리한다.

### 학습셋과 평가셋 분리 (오염 방지)

오탐률은 우리 서비스의 핵심 지표다. 학습에 쓴 데이터로 측정하면 숫자가 예뻐지고, **그 숫자로 발표하면 거짓말이 된다.**

```
ml/
  data_generation/   합성 데이터 생성 — 여기서 나온 것이 학습·데모에 다 쓰인다
  eval/              평가용 — 학습에 절대 쓰지 않는다  ★
```

- `ml/eval/ambiguous_100.jsonl` — 모호한 사례 **100건**. 계좌번호 vs 주문번호·사번·송장번호·전화번호처럼 형태가 같은 값들.
- 이 파일은 **한 번 만들면 고치지 않는다.** 성능이 안 나온다고 평가셋을 손보는 순간 지표의 의미가 사라진다.
- 라벨은 **두 사람이 각자 매기고 어긋난 것만 상의**한다. 혼자 매기면 기준이 흔들린다.
- 학습 스크립트 맨 앞에 오염 검사를 넣는다:

```python
eval_hashes = {sha1(x["text"]) for x in load("ml/eval/ambiguous_100.jsonl")}
assert not (train_hashes & eval_hashes), "평가셋이 학습 데이터에 섞였다"
```

- 평가셋 100건의 구성 비율(진짜 개인정보 몇 건 / 헷갈리는 값 몇 건)을 문서에 적어둔다. 심사에서 물어본다.

### 데모 샘플

- 심사위원용 샘플 4개는 **고정**한다. 발표 전날에 바꾸지 않는다.
- 샘플 파일은 저장소에 커밋한다(가상 데이터이므로 안전하고, 없으면 데모가 안 된다).

---

## 4. 체크리스트

### 커밋할 때마다

- [ ] 실제 개인정보·API 키가 섞이지 않았나
- [ ] 화면에 나가는 문구에 오타가 없나
- [ ] 패키지를 추가했다면 `pyproject.toml` + `uv.lock`을 같이 커밋했나
- [ ] 노트북 출력을 지웠나
- [ ] 모델 가중치가 딸려가지 않았나

### 폴더 구조를 바꿨을 때

- [ ] 팀 채팅에 알렸나
- [ ] `git mv`를 썼나
- [ ] 남의 import를 깨뜨리지 않았나

### 9/20 제출 전

- [ ] 서비스 링크가 **다른 사람 컴퓨터**에서 열리나
- [ ] 샘플 4개가 전부 동작하나
- [ ] 모바일에서 가로 스크롤이 안 생기나
- [ ] 성능 지표(Precision/Recall/F1/오탐률)가 문서에 있나
- [ ] README에 사용한 AI 모델과 학습 방식이 적혀 있나
- [ ] **10/5까지 링크가 살아 있을 방법이 마련되어 있나**
